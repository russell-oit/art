<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   
	   xmlns:context="http://www.springframework.org/schema/context"
	   xmlns:p="http://www.springframework.org/schema/p"
	   xmlns:mvc="http://www.springframework.org/schema/mvc"
	   xmlns:cache="http://www.springframework.org/schema/cache"

	   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
          http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		  http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		  http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd
	">
	
	<!-- Scans the classpath of this application for @Components to deploy as beans -->
	<context:component-scan base-package="art"/>

	<!-- Configures the @Controller programming model -->
	<!-- don't add model attributes to redirect url -->
	<!-- enable custom formatters and converters -->
	<mvc:annotation-driven ignore-default-model-on-redirect="true"
						   conversion-service="conversionService"/>
	
	<!-- enable i18n -->
	<!-- Application Message Bundle -->
	<bean id="messageSource" 
		  class="org.springframework.context.support.ReloadableResourceBundleMessageSource"
		  p:basenames="classpath:i18n/ArtMessages"
		  p:defaultEncoding="UTF-8"
		  p:fallbackToSystemLocale="false"
		  p:useCodeAsDefaultMessage="true"/>
		
	<!-- enable dynamic changing of language -->
	<mvc:interceptors>    
		<bean class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor"
			  p:paramName="lang" />
	</mvc:interceptors>

	<!-- remember locale using cookies -->
	<bean id="localeResolver"
		  class="org.springframework.web.servlet.i18n.CookieLocaleResolver">
		<property name="defaultLocale" value="en" />
	</bean>
	<!-- enable i18n -->
	
	<bean id="viewResolver"
		  class="org.springframework.web.servlet.view.InternalResourceViewResolver"
		  p:prefix="/WEB-INF/jsp/"
		  p:suffix=".jsp" />
	
	<bean id="emailTemplateResolver" class="org.thymeleaf.templateresolver.ClassLoaderTemplateResolver">
		<property name="prefix" value="mail/" />
		<property name="templateMode" value="HTML" />
		<!--<property name="characterEncoding" value="UTF-8" />-->
		<property name="order" value="1" />
		<!-- Template cache is true by default. Set to false if you want -->
		<!-- templates to be automatically updated when modified.        -->
		<!--<property name="cacheable" value="true" />-->
	</bean>
	
	<bean id="templateEngine" class="org.thymeleaf.spring4.SpringTemplateEngine">  
		<property name="templateResolvers">
			<set>
				<ref bean="emailTemplateResolver"/>
			</set>
		</property>
	</bean>
	
	<!-- enable caching -->
	<!-- Enables the caching through annotations -->
	<cache:annotation-driven key-generator="cacheKeyGenerator" />
	<bean id="cacheKeyGenerator" class="art.utils.CacheKeyGenerator" />
	
	<!-- ehcache cache manager -->
	<bean id="cacheManager"
		  class="org.springframework.cache.ehcache.EhCacheCacheManager"
		  p:cacheManager-ref="ehcache"/>
	
	<bean id="ehcache" class="org.springframework.cache.ehcache.EhCacheManagerFactoryBean"
		  p:configLocation="classpath:ehcache.xml"
		  p:shared="true"/>
	<!-- ehcache cache manager -->
	
	<!-- No op cache manager -->
	<!-- to disable caching, delete or comment out the ehcache beans and enable the NoopCacheManager -->
	<!-- https://stackoverflow.com/questions/14205185/spring-cache-vs-cachemanger -->
	
	<!--	<bean id="cacheManager"
	class="org.springframework.cache.support.NoOpCacheManager"/>-->
	
	<!-- No op cache manager -->
	
	<!-- enable caching -->
	
	<bean id="multipartResolver"
		  class="org.springframework.web.multipart.commons.CommonsMultipartResolver"/>

	
</beans>

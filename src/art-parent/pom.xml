<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                      http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>net.sf.art</groupId>
	<artifactId>art-parent</artifactId>
	<version>1.0-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>art-parent</name>
	<url>http://art.sourceforge.net</url>
	<description>Parent project for ART. ART is A Reporting Tool.</description>

	<modules>
		<module>artdbcp</module>
		<module>artmail</module>
		<module>art</module>
	</modules>

	<scm>
		<connection>scm:hg:http://hg.code.sf.net/p/art/code</connection>
		<developerConnection>scm:hg:https://hg.code.sf.net/p/art/code</developerConnection>
		<url>http://hg.code.sf.net/p/art/code</url>
	</scm>

	<issueManagement>
		<system>SourceForge</system>
		<url>http://sourceforge.net/p/art/tickets/</url>
	</issueManagement>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

		<jdk.version>1.6</jdk.version>
	</properties>

	<build>
		<pluginManagement>
			<plugins>
				<!-- set compiler options e.g. show warnings, compile for jdk 1.6 -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>2.3.2</version>
					<configuration>
						<source>${jdk.version}</source>
						<target>${jdk.version}</target>
						<debug>true</debug>
						<debuglevel>source,lines,vars</debuglevel>
						<showWarnings>true</showWarnings>
						<showDeprecation>true</showDeprecation>
					</configuration>
				</plugin>

				<!-- ensure only jdk 1.6 apis are used in code -->
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>animal-sniffer-maven-plugin</artifactId>
					<version>1.9</version>
					<executions>
						<execution>
							<id>check-java16</id>
							<phase>test</phase>
							<goals>
								<goal>check</goal>
							</goals>
							<configuration>
								<signature>
									<groupId>org.codehaus.mojo.signature</groupId>
									<artifactId>java16</artifactId>
									<version>1.1</version>
								</signature>
							</configuration>
						</execution>
					</executions>
				</plugin>

				<!-- skip tests. none are defined -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-surefire-plugin</artifactId>
					<version>2.15</version>
					<configuration>
						<skipTests>true</skipTests>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>

		<plugins>
			<!-- animal-sniffer mainly critical at release time to ensure only java 
				1.6 apis are used in the code -->
			<!-- <plugin> <groupId>org.codehaus.mojo</groupId> <artifactId>animal-sniffer-maven-plugin</artifactId> 
				</plugin> -->
		</plugins>
	</build>

	<dependencyManagement>
		<dependencies>
			<!-- jpivot needs commons-digester 1.x -->
			<dependency>
				<groupId>commons-digester</groupId>
				<artifactId>commons-digester</artifactId>
				<version>1.7</version>
			</dependency>
			<!-- use dom4j that comes with jpivot -->
			<dependency>
				<groupId>dom4j</groupId>
				<artifactId>dom4j</artifactId>
				<version>1.3-jpivot</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

</project>